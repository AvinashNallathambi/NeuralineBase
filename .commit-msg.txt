feat: wire up telemedicine dashboard and add screen share + recording consent

- Install simple-peer dependency (fixes patient portal VideoRoom crash)
- Replace mock data in TelemedicinePage with real API calls:
  - Fetch telemedicine sessions via telemedicineService.listSessions()
  - Fetch analytics via telemedicineService.getAnalytics()
  - Waiting room now shows real sessions with status "waiting"
  - "Join Call" creates/finds session via findOrCreateForAppointment
    and navigates to the call page
  - Added session statistics card with real analytics data
  - Added refresh button and loading spinner
- Add screen share to TelemedicineCallPage:
  - toggleScreenShare() uses getDisplayMedia() and replaces video track
  - Handles browser "Stop sharing" UI
  - Emits screen-share-state via socket
  - Restores original camera track on stop
- Add recording consent UI to TelemedicineCallPage:
  - Provider can request consent via socket event
  - Patient sees Modal.confirm dialog to accept/decline
  - Consent status shown in header (Yes/Pending tags)
  - Stops recording if patient declines
  - Cleans up screen share stream on unmount

Generated with [Devin](https://cli.devin.ai/docs)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
