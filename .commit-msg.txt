fix: deploy.yml YAML parse error (quote if expression with ! operator)

The !(github.event_name...) expression was being interpreted as a YAML
tag instead of a GitHub Actions expression. Wrapped in ${{ }} to fix.

Also updated deprecated ::set-output to $GITHUB_OUTPUT.

Generated with [Devin](https://cli.devin.ai/docs)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
